# Generate new SSH key
# ssh-keygen -t ed25519 -C "adam.cellon@1up.health"

# Run SSH agent in background
# eval "$(ssh-agent -s)"

# Update ssh config
# [[ -f ~/.ssh/config ]] || touch ~/.ssh/config



# Add to keychain
# ssh-add --apple-use-keychain ~/.ssh/id_ed25519

# Sync with github
# gh auth login
